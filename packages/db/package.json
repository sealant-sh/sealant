{
  "name": "@sealant/db",
  "version": "0.0.0",
  "private": true,
  "description": "Shared SQLite database package for control-plane state",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./payloads": "./src/payloads.ts"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/run-migrations.ts",
    "dev": "exec drizzle-kit studio --config drizzle.config.ts",
    "lint": "oxlint .",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15",
    "@sealant/validators": "workspace:*",
    "drizzle-orm": "^0.44.7",
    "drizzle-zod": "^0.8.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@sealant/typescript": "workspace:*",
    "drizzle-kit": "^0.31.5"
  }
}
