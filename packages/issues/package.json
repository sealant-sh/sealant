{
  "name": "@sealant/issues",
  "version": "0.0.0",
  "private": true,
  "description": "Issue workflow imports, normalization, and board state helpers",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "oxlint .",
    "test": "vitest run",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@sealant/typescript": "workspace:*"
  }
}
