{
  "name": "@sealant/credentials",
  "version": "0.0.0",
  "private": true,
  "description": "Connected-account credentials: payload schemas, encryption at rest, and workspace injection planning",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "oxlint .",
    "test": "vitest run",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@sealant/typescript": "workspace:*"
  }
}
