# RabbitMQ

`@sealant/rabbitmq` is the business-agnostic AMQP transport package.

It currently provides:

- shared env schema for RabbitMQ connection settings
- connection/channel singleton lifecycle helpers
- generic JSON publish/consume helpers
- generic topology assertion helpers
