# Validators

`@sealant/validators` owns shared validation contracts used across app boundaries.

It currently provides:

- API request/response schemas for core route groups
- sandbox queue message schemas used between API and worker
- sandbox build payload schemas shared by API, worker, and persistence layers
