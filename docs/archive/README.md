# Archive

Retired documentation and planning artifacts, kept for reference. Nothing in this directory is
published to the docs site.

- `docs-site/` — the pre-public-release content of `apps/docs/contents`, which was written for repo
  contributors. The user-facing docs that replaced it live in `apps/docs/contents`; several of those
  pages were rewritten from sources in here.
