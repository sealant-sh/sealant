{
  "name": "@sealant/ssh-gateway",
  "version": "0.0.0",
  "private": true,
  "description": "SSH gateway service for workspace routing",
  "type": "module",
  "scripts": {
    "build": "BUNDLE_EXTERNAL=ssh2,cpu-features node ../../tooling/scripts/bundle-app.mjs",
    "dev": "dotenv -e ../../.env -- tsx watch src/index.ts",
    "lint": "oxlint .",
    "test": "vitest run",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sealant/runtime-client": "^0.4.0",
    "@sealant/runtime-protocol": "^0.4.0",
    "@sealant/validators": "workspace:*",
    "ssh2": "^1.17.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@bufbuild/protobuf": "^2.12.0",
    "@sealant/typescript": "workspace:*",
    "@types/ssh2": "^1.15.5",
    "dotenv-cli": "catalog:"
  }
}
