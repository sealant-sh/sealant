{
  "name": "@sealant/ssh-gateway",
  "version": "0.0.0",
  "private": true,
  "description": "SSH gateway service for sandbox routing",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "lint": "oxlint .",
    "test": "vitest run",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ssh2": "^1.17.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@sealant/typescript": "workspace:*",
    "@types/ssh2": "^1.15.5"
  }
}
