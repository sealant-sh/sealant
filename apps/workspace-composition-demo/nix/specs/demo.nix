# The demo app reuses the canonical example spec from the shared package so the
# runnable example and the reusable library stay in sync.
import ../../../../packages/workspace-composition/examples/opencode-with-home-manager.nix
