# Tooling

Shared developer tooling packages live here.

Examples that may land in this workspace:

- TypeScript base configs
- ESLint and Prettier configs
- Vitest, Tailwind, or build presets
- internal scripts used across workspaces
